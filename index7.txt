for (let i = 1; i <= 10; i++) {
  let row = '';
  for (let j = 1; j <= 10; j++) {
    row += ${i * j}\t; // добавляем результат умножения в строку с табуляцией
  }
  console.log(row); // выводим строку на консоль
}
